\* Expected to fail after five modeled faults. This keeps MaxFaults=5 from
\* becoming a decorative bound whose deepest instability path is unreachable.
CONSTANTS
  ModeledSurfaces = {"agent"}
  Scopes = {scope}
  ItemIDs = {item}
  EventIDs = {event}
  Digests = {digest}
  Workers = {worker}
  Capacity = 1
  MaxFence = 1
  MaxVersion = 1
  MaxFaults = 5

SPECIFICATION Spec

INVARIANT FaultBudgetRemaining
