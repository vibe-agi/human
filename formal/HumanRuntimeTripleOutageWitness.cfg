\* Expected to fail: reaching a state with all three parties down is a
\* coverage witness for HumanRuntimeTripleOutage.cfg, not a safety promise.
CONSTANTS
  ModeledSurfaces = {"agent"}
  Scopes = {scope}
  ItemIDs = {item}
  EventIDs = {event}
  Digests = {digest}
  Workers = {worker}
  Capacity = 1
  MaxFence = 1
  MaxVersion = 1
  MaxFaults = 3

SPECIFICATION Spec

INVARIANT SomePartyUp
