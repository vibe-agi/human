\* Theorem-3 experiment: machine fairness only, humans may never act.
\* Expected: all SAFETY passes; EventuallyTerminal FAILS (counterexample:
\* humans never move). This failure is the mechanized proof that liveness
\* -- and only liveness -- depends on the humans.
CONSTANTS
  MaxTurns = 3
  MaxLocalEdits = 1
  MaxRewinds = 1
SPECIFICATION NoHumanFairSpec
INVARIANTS
  TypeOK
  LatestValid
  AppliedReal
  SupersededOK
  RewindPendingOK
PROPERTIES
  EventuallyTerminal
